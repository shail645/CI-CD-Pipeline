name : hello world demo
on:
   push:
      branches: 
          main
  jobs:
  say hello:
    runs-on: ubuntu-latest
    steos:
      name: checkout code
      uses: actions/checkout

      name: Print Hello
      run: echo "Hello Github actions"      
